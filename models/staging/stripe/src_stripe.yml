version: 2

sources:
  - name: stripe
    database: dbtworkshop
    schema: stripe
    tables:
      - name: payment
        loaded_at_field: _batched_at
        freshness: 
          warn_after:
            count: 365
            period: day
          error_after:
            count: 366
            period: day
        